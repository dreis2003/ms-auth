spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/db_ikon_auth?currentSchema=public}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

logging:
  level:
    br.com.ikonbrasil.auth: INFO
