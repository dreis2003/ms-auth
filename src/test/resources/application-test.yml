spring:
  datasource:
    url: jdbc:tc:postgresql:17:///db_ikon_auth_test?currentSchema=public
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
    locations: classpath:db/migration
